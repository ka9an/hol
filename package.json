{
  "private": true,
  "name": "websocket-template",
  "version": "1.0.0",
  "description": "A template for kick starting a Cloudflare Workers project",
  "main": "index.js",
  "scripts": {
  	"w": "wrangler publish",
    "g": "git add . && git commit -m \"first commit\" && git push -u origin main -f"
  },
  "dependencies": {
  	    "@cloudflare/wrangler": "latest",
     "isomorphic-unfetch": "latest",
    "firebase-firestore-lite": "latest"
  }
}
